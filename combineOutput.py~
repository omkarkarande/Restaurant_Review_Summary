import sys
import os

if len(sys.argv) != 3:
        print ('USAGE: python combineOutput.py <path to food folder> <path to output file of feature_detector.py>')
        sys.exit(0)
directory_menus = os.path.dirname('output/')
if not os.path.isdir(directory_menus):
	os.makedirs(directory_menus)
files=os.listdir(sys.argv[2])
categoryoutput=open(sys.argv[2],"r")
lines=categoryoutput.readlines()
no=0
for f in files:
	foodfile=open(sys.argv[1]+f,"r")
	outfile=open("output/"+f,"w")
	for i range(no,no+5):
		
	
